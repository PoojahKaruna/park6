name: testingwindowmain
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mapbox_gl: ^0.16.0
  cupertino_icons: ^1.0.6
  flutter_map: ^6.1.0
  geolocator: ^11.0.0
  latlong2: ^0.9.0
  flutter_map_cancellable_tile_provider: ^2.0.0
  flutter_map_location_marker: ^8.0.8




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo1.png